import { ChangeColorTypeDirective } from './change-color-type.directive';

describe('ChangeColorTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorTypeDirective();
    expect(directive).toBeTruthy();
  });
});
